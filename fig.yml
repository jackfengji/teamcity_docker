server:
    build: server
    volumes:
        - ./data:/data/teamcity
    ports:
        - "8111:8111"

agent:
    build: agent
    links:
        - server
    environment:
        - DEV_UID=1001
        - DEV_GID=1001
    volumes:
        - /home/jackfengji/android-sdk:/opt/android-sdk
        - ./agent/AgentSrc:/opt/teamcity-agent
